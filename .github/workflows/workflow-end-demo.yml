name: workflow-end-demo
on:
  push:
    branches: [localdev-bender-e2e]
    paths: [".github/workflows/workflow-end-demo*.yml"]
jobs:
  build:
    runs-on: localdev-bender-e2e
    steps:
      - run: '[ "${{ github.run_attempt }}" != 1 ] || { echo "attempt 1 fails so it can be re-run"; exit 1; }'
