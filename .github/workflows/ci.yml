name: CI
on:
    push:
        branches: [localdev-nacho-main ]
    pull_request:
        branches: [localdev-nacho-main ]
jobs:
    ls:
        runs-on: localdev-nacho-main
        steps:
            - name: LS
              run: |
                ls -la

    xxx:
        uses: .github/workflows/ci2.yml@localdev-nacho-main
        
