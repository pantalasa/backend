name: lunar-e2e-pin-rotation-ae6368491
on:
  pull_request:
    types: [opened, labeled]
permissions:
  contents: read
jobs:
  ready:
    if: github.event.action == 'opened'
    runs-on: ubuntu-24.04
    steps:
      - run: /bin/echo 'pin rotation PR opened ae6368491'
  ci:
    if: github.event.action == 'labeled' && github.event.label.name == 'e2e-pin-rotation-ae6368491'
    runs-on: localdev-e2e-test-36212512719-2-github-1-e2e
    steps:
      - run: /bin/echo 'pin rotation CI ae6368491'
